<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MkononiAppTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="textViewtextColor" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="clickableTextView" parent="Widget.AppCompat.TextView">
        <item name="android:padding">12dp</item>
        <item name="minHeight">40dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="MainToolbarStyle" parent="Theme.MkononiAppTest">
        <item name="android:textColorSecondary">@color/white</item>
        <item name="toolbarStyle">@style/ToolbarStyling</item>
    </style>
    <style name="ToolbarStyling" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleCentered">true</item>
        <item name="titleTextColor">@color/white</item>
    </style>
</resources>